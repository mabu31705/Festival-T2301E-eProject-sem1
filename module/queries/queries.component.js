angular.module('queries').component('queries', {
    templateUrl: './module/queries/queries.template.html',
    controller: ['$routeParams', '$scope', '$window',
        function queriesController($routeParams, $scope, $window) {
        }
    ]
});
