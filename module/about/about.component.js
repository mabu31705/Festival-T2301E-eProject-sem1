angular.module('about').component('about', {
    templateUrl: './module/about/about.template.html',
    controller: ['$routeParams', '$scope', '$window',
        function aboutController($routeParams, $scope, $window) {
        }
    ]
});
